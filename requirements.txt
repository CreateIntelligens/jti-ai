google-genai>=1.0.0
python-dotenv>=1.0.0
fastapi>=0.115.0
uvicorn>=0.32.0
python-multipart>=0.0.12
pymongo[srv]>=4.0.0
redis>=5.0.0
openpyxl>=3.1.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
opencc-python-reimplemented>=0.1.7
lancedb>=0.12.0
bcrypt>=4.0.0
PyJWT>=2.8.0
cryptography>=42.0.0

# 測試（dev/CI 用；輕量純 Python，留在 image 內方便容器內跑 pytest）
pytest>=8.0.0
pytest-mock>=3.14.0

# 注意：重相依（torch / transformers / accelerate / sentence-transformers /
# FlagEmbedding / tf-keras）已移到 docker/backend.Dockerfile 的獨立 RUN 層，
# 放在 COPY requirements.txt 之前以保住 build cache。
# 若改用非 Docker 環境，請另行安裝這些套件。
