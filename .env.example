# ===== Gemini =====
# 逗號分隔多把 key（名稱:key），啟動時自動掃 stores 建 mapping。取得：https://aistudio.google.com/apikey
GEMINI_API_KEYS=YOUR_KEY_1,YOUR_KEY_2
GEMINI_MODEL_NAME=gemini-3.1-flash-lite
# app → key 綁定（精確比對，名稱須與 GEMINI_API_KEYS 逐字一致）
APP_KEY_MAP=jti:JTI傑太日煙,hciot:HCIOT護聯

# ===== Server =====
# MODE 只能是 dev 或 prod。dev → uvicorn --reload（單進程熱重載）；prod → --workers 2。未設定時預設 prod。
MODE=prod
PORT=8008
ADMIN_API_KEY=your-admin-key-here

# ===== MongoDB =====
# 主庫：AWS DocumentDB（經 db-tunnel 容器）。走 tunnel 必帶 tlsAllowInvalidHostnames + directConnection
MONGODB_URI=mongodb://docdbadmin:XXXXXXXX@db-tunnel:27017/?tls=true&tlsCAFile=/app/global-bundle.pem&tlsAllowInvalidHostnames=true&directConnection=true&retryWrites=false
# 備援：Atlas，主庫不可用時啟動自動 fallback（留空則停用）。fallback 期間資料需用 scripts/merge_atlas_to_documentdb.py 補回主庫
MONGODB_URI_FALLBACK=mongodb+srv://XXXX_db_user:XXXXXXXXX@poc0.rsin8mu.mongodb.net/?appName=poc0

# ===== db-tunnel =====
DOCDB_ENDPOINT=us-west-2-mongodb-cluster.cluster-XXXXXXXX.us-west-2.docdb.amazonaws.com
DOCDB_PORT=27017
# 跳板機（僅 VPC 外部署需要）
BASTION_HOST=XX.XX.XX.XX
BASTION_USER=ec2-user

# ===== Secrets =====
SESSION_JWT_SECRET=
# Fernet：可逆加密 sk-xxx 供事後 reveal。產生：python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
API_ENCRYPTION_KEY=

# ===== TTS / 正規化 =====
TTS_API_URL=http://10.9.0.35:8001/tts_stream
TTS_TIMEOUT_SECONDS=20
TTS_CACHE_TTL_SECONDS=900
TTS_MAX_JOBS=500
# 設定後優先呼叫 API（數字轉中文 + 繁→簡），失敗 fallback 本地 regex
NORMALIZE_API_URL=http://10.9.0.35:8956/normalize

# ===== JTI =====
JTI_TTS_CHARACTER=hayley

# ===== HCIoT =====
HCIOT_TTS_CHARACTER=healthy2
# header 右側「TTS 管理後台」按鈕（留空則不顯示）
VITE_HCIOT_EXTERNAL_LINK_URL=

# ===== Frontend =====
VITE_PUBLIC_ALLOWED_PAGES=home,hciot,jti
VITE_PUBLIC_RESTRICTED_HOSTS=123.asd.ai
